// lib/constants/colors.dart
import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFE53935);      // Red (Appetite stimulant & primary brand color)
  static const accent = Color(0xFFFF7043);       // Warm Orange (Call to action or highlights)
  static const background = Color(0xFFF5F5F5);   // Light Beige (App background)
  static const cardBackground = Color(0xFFFFF3E0); // Soft Beige (Cards and containers)
  static const white = Color(0xFFFFFFFF);        // Clean background or text
  static const darkText = Color(0xFF333333);     // Primary dark text
  static const lightGray = Color(0xFFB0BEC5);    // For borders, subtle text
  static const success = Color(0xFF66BB6A);      // For success messages (like Delivered)
}
